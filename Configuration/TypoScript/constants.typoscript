# Overrides für View-Pfade (leer lassen oder im Projekt überschreiben)
container_package.view {
    templateRootPaths.20 =
    partialRootPaths.20  =
    layoutRootPaths.20   =
}

# Assets overridebar machen
container_package.assets.cssFile = EXT:container_package/Resources/Public/Css/container_package.css