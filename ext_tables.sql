#
# Add SQL definition of database tables
#

CREATE TABLE tt_content (
    tx_container_package_be_cols  int(11) unsigned DEFAULT '1' NOT NULL,
    tx_container_package_be_color varchar(10)      DEFAULT ''  NOT NULL
);
